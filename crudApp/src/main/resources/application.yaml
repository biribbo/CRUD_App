spring:
  h2.console:
   enabled: true
   path: "/console"
  datasource:
    url: jdbc:h2:file:./product-db
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true